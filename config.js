/* ─────────────────────────────────────────────
   Configuración del contador de "me gusta" (Supabase).
   Pegá acá los datos de tu proyecto (UNA sola vez).
   El "anon key" es público, no es un secreto.
   Si lo dejás en blanco, los corazones funcionan igual
   pero sin contador compartido.
   ───────────────────────────────────────────── */
const GALLERY_CONFIG = {
  SUPABASE_URL: "",        // ej: https://abcd1234.supabase.co
  SUPABASE_ANON_KEY: ""    // tu anon/public key
};
